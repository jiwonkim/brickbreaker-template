$(document).ready(function() {
    var canvas = document.getElementById('canvas');
    var context = canvas.getContext('2d');

    /* TODO STAGE 1 */

    // 1. Draw a rectangle

    // 2. Save the rectangle's x, y coordinates and width and height
    //    in variables

    $(canvas).click(function(evt) {
        // 3. get the x and y offsets from the evt
        // 4. check against the saved x, y, w, h variables and see if the
        //    click was inside the rectangle
        // 5. POST to the save_score endpoint using ajax
    });
});
